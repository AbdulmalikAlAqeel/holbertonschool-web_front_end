# Advanced CSS - Project Overview

Welcome to the **CSS Advanced** project! This project focuses on building a modern, responsive, and visually appealing web page using advanced CSS techniques, structured semantic HTML5, and clean web architecture.

---

## 📌 Project Description

In this initial setup (**Task 0**), we established the foundational files and visual assets required for the **Techium** landing page. The goal is to build a full-featured homepage utilizing custom styles, responsive layouts, design tokens, and modular structure.

---

## 🛠️ Requirements & Standards

* **HTML Validation:** W3C compliant HTML5.
* **Compatibility:** Tested for modern browser rendering (Chrome 78+).
* **Code Format:** All files end with a newline and include appropriate comments.
* **Assets Organization:** All project images and visual assets stored cleanly inside the `images/` directory.

---

## 📁 Directory Structure

```text
CSS_advanced/
├── README.md              # Project documentation
├── index.html             # Base HTML structure for Techium homepage
└── images/                # Project graphics and photography assets
    ├── favicon.jpg
    ├── logo-black.png
    ├── logo-white.png
    ├── pic-about-01.jpg   # About Us section asset
    ├── pic-work-01.jpg    # Interior Design work sample
    ├── pic-work-02.jpg    # Web Development work sample
    ├── pic-work-03.jpg    # Personal Development work sample
    ├── pic-article-01.jpg # Article: Career
    ├── pic-article-02.jpg # Article: Digital Life
    ├── pic-article-03.jpg # Article: Social
    ├── pic-person-01.jpg  # Testimonial Avatar: Yuri Y.
    ├── pic-person-02.jpg  # Testimonial Avatar: Dorrie S.
    └── pic-person-03.jpg  # Testimonial Avatar: Sven H.



# Advanced CSS - Project Overview

Welcome to the **CSS Advanced** project! This repository contains a series of tasks designed to build a modern, responsive, and visually polished website for **Techium** using advanced CSS features, modular architecture, and W3C-compliant HTML5.

---

## 📌 Project Tasks Overview

### Task 0: Base HTML & Image Assets
* Established the foundational `index.html` file using structured HTML5 markup.
* Downloaded and organized all required media assets and high-resolution images in the `images/` directory.

### Task 1: Smooth Scrolling (Effortless Transitions)
* Created the primary stylesheet structure inside `styles/1-style.css`.
* Applied smooth scrolling behavior across the entire document using `scroll-behavior: smooth` on the `html` selector.

---

## 🛠️ Technical Standards & Requirements

* **W3C Validation:** HTML files pass W3C standards without errors or warnings.
* **Browsers:** Optimized for modern Chrome versions (Chrome 78+).
* **Code Format:** Standard CSS/HTML layout, with all files ending in a newline.
* **Structure:** Clean separation of concerns with HTML structure at the root and stylesheets stored in `styles/`.

---

## 📁 Directory Structure

```text
CSS_advanced/
├── README.md              # Project documentation
├── index.html             # Base HTML structure for Techium homepage
├── images/                # High-res photography & brand logo assets
│   ├── favicon.jpg
│   ├── logo-black.png
│   ├── logo-white.png
│   ├── pic-about-01.jpg
│   ├── pic-work-01.jpg
│   ├── pic-work-02.jpg
│   ├── pic-work-03.jpg
│   ├── pic-article-01.jpg
│   ├── pic-article-02.jpg
│   ├── pic-article-03.jpg
│   ├── pic-person-01.jpg
│   ├── pic-person-02.jpg
│   └── pic-person-03.jpg
└── styles/                # CSS Stylesheets directory
    └── 1-style.css        # Stylesheet for Task 1 (Smooth scrolling)



# Advanced CSS - Project Overview

Welcome to the **CSS Advanced** project! This repository contains a series of tasks designed to build a modern, responsive, and visually polished website for **Techium** using advanced CSS features, design systems, and W3C-compliant HTML5.

---

## 📌 Project Tasks Overview

### Task 0: Base HTML & Image Assets
* Established the foundational `index.html` file using structured HTML5 markup.
* Downloaded and organized all required media assets and high-resolution images in the `images/` directory.

### Task 1: Smooth Scrolling (Effortless Transitions)
* Created the primary stylesheet structure inside `styles/1-style.css`.
* Applied smooth scrolling behavior across the entire document using `scroll-behavior: smooth` on the `html` selector.

### Task 2: Color Values & Display Rules
* Created `styles/2-style.css` extending the smooth scroll rules.
* Configured core text foreground colors (`color: #161616`) for `body` and anchor (`a`) tags.
* Defined element visibility rules by setting `display: none` on `.visually-hidden`.
* Applied primary brand accent colors (`color: #D73953`) to `.card-category` and `.section-tagline`.

---

## 🛠️ Technical Standards & Requirements

* **W3C Validation:** HTML files pass W3C standards without errors or warnings.
* **Browsers:** Optimized for modern Chrome versions (Chrome 78+).
* **Code Format:** Standard CSS/HTML layout, with all files ending in a newline.
* **Structure:** Clean separation of concerns with HTML structure at the root and stylesheets stored in `styles/`.

---

## 📁 Directory Structure

```text
CSS_advanced/
├── README.md              # Project documentation
├── index.html             # Base HTML structure for Techium homepage
├── images/                # High-res photography & brand logo assets
│   ├── favicon.jpg
│   ├── logo-black.png
│   ├── logo-white.png
│   ├── pic-about-01.jpg
│   ├── pic-work-01.jpg
│   ├── pic-work-02.jpg
│   ├── pic-work-03.jpg
│   ├── pic-article-01.jpg
│   ├── pic-article-02.jpg
│   ├── pic-article-03.jpg
│   ├── pic-person-01.jpg
│   ├── pic-person-02.jpg
│   └── pic-person-03.jpg
└── styles/                # CSS Stylesheets directory
    ├── 1-style.css        # Stylesheet for Task 1 (Smooth scrolling)
    └── 2-style.css        # Stylesheet for Task 2 (Colors & Display rules)



# Advanced CSS - Project Overview

Welcome to the **CSS Advanced** project! This repository contains a series of tasks designed to build a modern, responsive, and visually polished website for **Techium** using advanced CSS features, design systems, custom properties (variables), and W3C-compliant HTML5.

---

## 📌 Project Tasks Overview

### Task 0: Base HTML & Image Assets
* Established the foundational `index.html` file using structured HTML5 markup.
* Downloaded and organized all required media assets and high-resolution images in the `images/` directory.

### Task 1: Smooth Scrolling (Effortless Transitions)
* Created the primary stylesheet structure inside `styles/1-style.css`.
* Applied smooth scrolling behavior across the entire document using `scroll-behavior: smooth` on the `html` selector.

### Task 2: Color Values & Display Rules
* Created `styles/2-style.css` extending the smooth scroll rules.
* Configured core text foreground colors (`color: #161616`) for `body` and anchor (`a`) tags.
* Defined element visibility rules by setting `display: none` on `.visually-hidden`.
* Applied primary brand accent colors (`color: #D73953`) to `.card-category` and `.section-tagline`.

### Task 3: CSS Custom Properties (Variables)
* Created `styles/3-style.css` introducing CSS Variables under the `:root` pseudo-class.
* Declared color palette variables: `--color-primary`, `--color-black`, `--color-white`, `--color-light-grey`, and `--color-dark-grey`.
* Defined dynamic alias variable `--text-color: var(--color-black)`.
* Refactored `body`, `a`, `.card-category`, and `.section-tagline` declarations to use `var()` custom properties for enhanced maintainability.

---

## 🛠️ Technical Standards & Requirements

* **W3C Validation:** HTML files pass W3C standards without errors or warnings.
* **Browsers:** Optimized for modern Chrome versions (Chrome 78+).
* **Code Format:** Standard CSS/HTML layout, with all files ending in a newline.
* **Structure:** Clean separation of concerns with HTML structure at the root and stylesheets stored in `styles/`.

---

## 📁 Directory Structure

```text
CSS_advanced/
├── README.md              # Project documentation
├── index.html             # Base HTML structure for Techium homepage
├── images/                # High-res photography & brand logo assets
│   ├── favicon.jpg
│   ├── logo-black.png
│   ├── logo-white.png
│   ├── pic-about-01.jpg
│   ├── pic-work-01.jpg
│   ├── pic-work-02.jpg
│   ├── pic-work-03.jpg
│   ├── pic-article-01.jpg
│   ├── pic-article-02.jpg
│   ├── pic-article-03.jpg
│   ├── pic-person-01.jpg
│   ├── pic-person-02.jpg
│   └── pic-person-03.jpg
└── styles/                # CSS Stylesheets directory
    ├── 1-style.css        # Task 1: Smooth scrolling
    ├── 2-style.css        # Task 2: Hardcoded color declarations
    └── 3-style.css        # Task 3: CSS Custom Properties (Variables)



# Advanced CSS - Project Overview

Welcome to the **CSS Advanced** project! This repository contains a series of tasks designed to build a modern, responsive, and visually polished website for **Techium** using advanced CSS features, design systems, custom properties (variables), typography stack, and W3C-compliant HTML5.

---

## 📌 Project Tasks Overview

### Task 0: Base HTML & Image Assets
* Established the foundational `index.html` file using structured HTML5 markup.
* Downloaded and organized all required media assets and high-resolution images in the `images/` directory.

### Task 1: Smooth Scrolling (Effortless Transitions)
* Created the primary stylesheet structure inside `styles/1-style.css`.
* Applied smooth scrolling behavior across the entire document using `scroll-behavior: smooth` on the `html` selector.

### Task 2: Color Values & Display Rules
* Created `styles/2-style.css` extending the smooth scroll rules.
* Configured core text foreground colors (`color: #161616`) for `body` and anchor (`a`) tags.
* Defined element visibility rules by setting `display: none` on `.visually-hidden`.
* Applied primary brand accent colors (`color: #D73953`) to `.card-category` and `.section-tagline`.

### Task 3: CSS Custom Properties (Variables)
* Created `styles/3-style.css` introducing CSS Variables under the `:root` pseudo-class.
* Declared color palette variables: `--color-primary`, `--color-black`, `--color-white`, `--color-light-grey`, and `--color-dark-grey`.
* Defined dynamic alias variable `--text-color: var(--color-black)`.
* Refactored `body`, `a`, `.card-category`, and `.section-tagline` declarations to use `var()` custom properties for enhanced maintainability.

### Task 4: Typography Custom Properties
* Created `styles/4-style.css` defining font stack variables under `:root`.
* Declared `--font-family-base` and `--font-family-title` with fallback font chains (`"Open Sans"`, `"Raleway"`, `"Helvetica Neue"`, `Helvetica`, `Arial`, `sans-serif`).
* Applied `--font-family-base` to the `body` selector.
* Targeted all six heading levels (`h1, h2, h3, h4, h5, h6`) applying `--font-family-title`.

---

## 🛠️ Technical Standards & Requirements

* **W3C Validation:** HTML files pass W3C standards without errors or warnings.
* **Browsers:** Optimized for modern Chrome versions (Chrome 78+).
* **Code Format:** Standard CSS/HTML layout, with all files ending in a newline.
* **Structure:** Clean separation of concerns with HTML structure at the root and stylesheets stored in `styles/`.

---

## 📁 Directory Structure

```text
CSS_advanced/
├── README.md              # Project documentation
├── index.html             # Base HTML structure for Techium homepage
├── images/                # High-res photography & brand logo assets
│   ├── favicon.jpg
│   ├── logo-black.png
│   ├── logo-white.png
│   ├── pic-about-01.jpg
│   ├── pic-work-01.jpg
│   ├── pic-work-02.jpg
│   ├── pic-work-03.jpg
│   ├── pic-article-01.jpg
│   ├── pic-article-02.jpg
│   ├── pic-article-03.jpg
│   ├── pic-person-01.jpg
│   ├── pic-person-02.jpg
│   └── pic-person-03.jpg
└── styles/                # CSS Stylesheets directory
    ├── 1-style.css        # Task 1: Smooth scrolling
    ├── 2-style.css        # Task 2: Hardcoded color declarations
    ├── 3-style.css        # Task 3: Color Variables
    └── 4-style.css        # Task 4: Font-family Variables



# Advanced CSS - Project Overview

Welcome to the **CSS Advanced** project! This repository contains a series of tasks designed to build a modern, responsive, and visually polished website for **Techium** using advanced CSS features, design systems, custom properties (variables), typography scale, and W3C-compliant HTML5.

---

## 📌 Project Tasks Overview

### Task 0: Base HTML & Image Assets
* Established the foundational `index.html` file using structured HTML5 markup.
* Downloaded and organized all required media assets and high-resolution images in the `images/` directory.

### Task 1: Smooth Scrolling (Effortless Transitions)
* Created the primary stylesheet structure inside `styles/1-style.css`.
* Applied smooth scrolling behavior across the entire document using `scroll-behavior: smooth` on the `html` selector.

### Task 2: Color Values & Display Rules
* Created `styles/2-style.css` extending the smooth scroll rules.
* Configured core text foreground colors (`color: #161616`) for `body` and anchor (`a`) tags.
* Defined element visibility rules by setting `display: none` on `.visually-hidden`.
* Applied primary brand accent colors (`color: #D73953`) to `.card-category` and `.section-tagline`.

### Task 3: CSS Custom Properties (Variables)
* Created `styles/3-style.css` introducing CSS Variables under the `:root` pseudo-class.
* Declared color palette variables: `--color-primary`, `--color-black`, `--color-white`, `--color-light-grey`, and `--color-dark-grey`.
* Defined dynamic alias variable `--text-color: var(--color-black)`.
* Refactored `body`, `a`, `.card-category`, and `.section-tagline` declarations to use `var()` custom properties for enhanced maintainability.

### Task 4: Typography Custom Properties
* Created `styles/4-style.css` defining font stack variables under `:root`.
* Declared `--font-family-base` and `--font-family-title` with fallback font chains (`"Helvetica Neue"`, `Helvetica`, `Arial`, `sans-serif`).
* Applied `--font-family-base` to the `body` selector.
* Targeted all six heading levels (`h1, h2, h3, h4, h5, h6`) applying `--font-family-title`.

### Task 5: Typography Sizing Scale
* Created `styles/5-style.css` establishing a scalable REM font size system.
* Set `font-size: 62.5%` on the `html` selector to simplify REM calculations ($1\text{rem} = 10\text{px}$).
* Declared font size custom properties under `:root`:
  * `--font-size-small: 1.2rem`
  * `--font-size-medium: 1.6rem`
  * `--font-size-large: 1.8rem`
  * `--font-size-x-large: 2.3rem`
  * `--font-size-xx-large: 4.8rem`
* Set base `font-size` for `body` using `var(--font-size-medium)`.

---

## 🛠️ Technical Standards & Requirements

* **W3C Validation:** HTML files pass W3C standards without errors or warnings.
* **Browsers:** Optimized for modern Chrome versions (Chrome 78+).
* **Code Format:** Standard CSS/HTML layout, with all files ending in a newline.
* **Structure:** Clean separation of concerns with HTML structure at the root and stylesheets stored in `styles/`.

---

## 📁 Directory Structure

```text
CSS_advanced/
├── README.md              # Project documentation
├── index.html             # Base HTML structure for Techium homepage
├── images/                # High-res photography & brand logo assets
│   ├── favicon.jpg
│   ├── logo-black.png
│   ├── logo-white.png
│   ├── pic-about-01.jpg
│   ├── pic-work-01.jpg
│   ├── pic-work-02.jpg
│   ├── pic-work-03.jpg
│   ├── pic-article-01.jpg
│   ├── pic-article-02.jpg
│   ├── pic-article-03.jpg
│   ├── pic-person-01.jpg
│   ├── pic-person-02.jpg
│   └── pic-person-03.jpg
└── styles/                # CSS Stylesheets directory
    ├── 1-style.css        # Task 1: Smooth scrolling
    ├── 2-style.css        # Task 2: Hardcoded color declarations
    ├── 3-style.css        # Task 3: Color Variables
    ├── 4-style.css        # Task 4: Font-family Variables
    └── 5-style.css        # Task 5: Font-size Scale & Base Rules



# Advanced CSS - Project Overview

Welcome to the **CSS Advanced** project! This repository contains a series of tasks designed to build a modern, responsive, and visually polished website for **Techium** using advanced CSS features, design systems, custom properties (variables), typography rules, and W3C-compliant HTML5.

---

## 📌 Project Tasks Overview

### Task 0: Base HTML & Image Assets
* Established the foundational `index.html` file using structured HTML5 markup.
* Downloaded and organized all required media assets and high-resolution images in the `images/` directory.

### Task 1: Smooth Scrolling (Effortless Transitions)
* Created the primary stylesheet structure inside `styles/1-style.css`.
* Applied smooth scrolling behavior across the entire document using `scroll-behavior: smooth` on the `html` selector.

### Task 2: Color Values & Display Rules
* Created `styles/2-style.css` extending the smooth scroll rules.
* Configured core text foreground colors (`color: #161616`) for `body` and anchor (`a`) tags.
* Defined element visibility rules by setting `display: none` on `.visually-hidden`.
* Applied primary brand accent colors (`color: #D73953`) to `.card-category` and `.section-tagline`.

### Task 3: CSS Custom Properties (Variables)
* Created `styles/3-style.css` introducing CSS Variables under the `:root` pseudo-class.
* Declared color palette variables: `--color-primary`, `--color-black`, `--color-white`, `--color-light-grey`, and `--color-dark-grey`.
* Defined dynamic alias variable `--text-color: var(--color-black)`.
* Refactored `body`, `a`, `.card-category`, and `.section-tagline` declarations to use `var()` custom properties for enhanced maintainability.

### Task 4: Typography Custom Properties
* Created `styles/4-style.css` defining font stack variables under `:root`.
* Declared `--font-family-base` and `--font-family-title` with fallback font chains (`"Helvetica Neue"`, `Helvetica`, `Arial`, `sans-serif`).
* Applied `--font-family-base` to the `body` selector.
* Targeted all six heading levels (`h1, h2, h3, h4, h5, h6`) applying `--font-family-title`.

### Task 5: Typography Sizing Scale
* Created `styles/5-style.css` establishing a scalable REM font size system.
* Set `font-size: 62.5%` on the `html` selector to simplify REM calculations ($1\text{rem} = 10\text{px}$).
* Declared font size custom properties under `:root`:
  * `--font-size-small: 1.2rem`
  * `--font-size-medium: 1.6rem`
  * `--font-size-large: 1.8rem`
  * `--font-size-x-large: 2.3rem`
  * `--font-size-xx-large: 4.8rem`
* Set base `font-size` for `body` using `var(--font-size-medium)`.

### Task 6: Typography Weight Custom Properties
* Created `styles/6-style.css` adding font weight variables under `:root`.
* Declared `--font-weight-regular: 400` and `--font-weight-bold: 700`.
* Applied `--font-weight-regular` to the `body` selector.
* Applied `--font-weight-bold` to all heading levels (`h1, h2, h3, h4, h5, h6`).

---

## 🛠️ Technical Standards & Requirements

* **W3C Validation:** HTML files pass W3C standards without errors or warnings.
* **Browsers:** Optimized for modern Chrome versions (Chrome 78+).
* **Code Format:** Standard CSS/HTML layout, with all files ending in a newline.
* **Structure:** Clean separation of concerns with HTML structure at the root and stylesheets stored in `styles/`.

---

## 📁 Directory Structure

```text
CSS_advanced/
├── README.md              # Project documentation
├── index.html             # Base HTML structure for Techium homepage
├── images/                # High-res photography & brand logo assets
│   ├── favicon.jpg
│   ├── logo-black.png
│   ├── logo-white.png
│   ├── pic-about-01.jpg
│   ├── pic-work-01.jpg
│   ├── pic-work-02.jpg
│   ├── pic-work-03.jpg
│   ├── pic-article-01.jpg
│   ├── pic-article-02.jpg
│   ├── pic-article-03.jpg
│   ├── pic-person-01.jpg
│   ├── pic-person-02.jpg
│   └── pic-person-03.jpg
└── styles/                # CSS Stylesheets directory
    ├── 1-style.css        # Task 1: Smooth scrolling
    ├── 2-style.css        # Task 2: Color declarations
    ├── 3-style.css        # Task 3: Color Variables
    ├── 4-style.css        # Task 4: Font-family Variables
    ├── 5-style.css        # Task 5: Font-size Scale
    └── 6-style.css        # Task 6: Font-weight Variables
